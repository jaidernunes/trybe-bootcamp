const result = Math.floor((Math.random() * 5) +1);

const rng5 = (bet, result) => {
  console.log(result)
  if (bet === result) {
    return 'You Win!'
  }
  if (bet !== result) {
    return 'You Lose!'
  }
}
console.log(rng5(3, result))